# CoAll Evolution Convergence R1

**State:** `CURRENT_MAIN_FANIN_CANDIDATE__CI_PENDING__NO_RUNTIME_NO_CANON_NO_AUTHORITY`

R1 forward-ports the useful machine contract from draft PR #44 onto current main rather than replaying its stale branch history.

Observed main head for this fan-in:

`9d688da292c9c6c392661809131c5b64f47b5680`

## What is forward-ported

Exact donor blobs are preserved for:

- the additive CoEvoDelta v0.2 union candidate;
- the elected machine intake policy;
- the deterministic convergence validator;
- the read-only GitHub Actions validator workflow.

The union keeps the v0.1 required/core properties unchanged while carrying:

- PR #32 effect typing;
- PR #37 materiality / subscription / CoPressure / CoEnerget / CoWant / route / close-readiness fields;
- PR #23 write-back and projection-provenance fields.

`SCHEMA_UNION_CANDIDATE_NE_SCHEMA_ACCEPTANCE`

## Intake election

Primary machine intake:

`ai/evolution-deltas/<YYYY-MM-DD>/`

Human-readable material under `docs/Evolution/deltas/` remains a documentation/index/example projection, not a second semantic ownership root.

`ONE_DELTA_ID_ONE_PRIMARY_MACHINE_OBJECT__MULTIPLE_READABLE_PROJECTIONS_ALLOWED`

## Descendant satisfaction since the donor branch

Current main has independently landed substantial relations that shrink older donor scope:

- CoPulse R0G local receiver/capacity-source provenance;
- CoEvidence receipt guard;
- CoLineage non-destructive compaction;
- CoSubscription ecology;
- CoLivingHighlight identity/read-cursor architecture;
- CoSourceGraph;
- participant-owned CoNode contract;
- CoSubstrateField and machine-readable living-highlight asset contract.

Those descendants should be checked before replaying older broad PRs.

`DESCENDANT_SATISFACTION_BEFORE_REPLAY`

## Why this does not merge the old donor PRs wholesale

Their branches encode useful history, but many began from substantially older mainline states and overlap one another. Mainline fan-in should carry unique relations while refusing last-writer-wins and refusing a repository-count definition of progress.

`FANIN_BEFORE_MORE_PARALLEL_MUTATION`  
`PR_COUNT_NE_PROGRESS`  
`NO_NEWEST_WINS`

## Gate

The newly forward-ported validator workflow is read-only and must pass on this fresh branch before merge election.

A CI PASS proves only contract consistency for the bounded validator scope. It does not establish canon, runtime adoption, integration, CoEx, or authority.

`CI_PASS_NE_CANON_OR_MERGE_AUTHORITY`

After PASS and a current-head recheck, this fan-in is eligible for a low-effect merge. Then older donor PRs can be reclassified as:

- descendant-satisfied and closeable;
- unique donor still worth fan-in;
- historical/negative evidence;
- superseded by current main.

## Nonclaims

No runtime, provider-session, credential, public-outreach, financial, privacy-sensitive, destructive, canon, CoEx, or authority effect is performed by this convergence artifact.
