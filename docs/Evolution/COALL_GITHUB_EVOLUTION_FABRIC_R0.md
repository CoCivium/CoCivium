# CoAll GitHub Evolution Fabric R0

**State:** `PUBLIC_SHARED_EVOLUTION_CONTRACT__NO_GLOBAL_SESSION_ACK_CLAIM`

## Lead

GitHub is now an elected shared public/currentness/evolution surface for CoCivium, not merely a place to park code.

Every capable session should be able to **contribute bounded deltas** to the evolving project field without carrying the whole project in its context window.

The default is not “every session edits everything.”

The default is:

`ORIENT -> SUBSCRIBE -> OBSERVE -> PROPOSE -> CHALLENGE -> MUTATE_SCOPED_BRANCH -> VERIFY -> MERGE_WHERE_ALLOWED -> PULSE_CURRENTNESS`

`ALL_SESSIONS_CAN_CONTRIBUTE_NE_ALL_SESSIONS_MUTATE_ALL_SURFACES`

## Evolution domains

The initial shared domain field includes, but is not limited to:

- `CoStrategy+`
- `CoInsight+`
- `CoTheoryAll+`
- `CoHighlight+`
- `CoIndex+`
- `CoLex+`
- `CoAll+`
- `CoOps+`
- `CoMythOps+`
- README/documentation ecology
- repository/folder/topology projections
- architecture/substrate relations
- `CoPriMath+`
- `CoGibberTru+`
- `RickBar+ / CoDesktop+`
- UX / surface / projection relations
- humour / memeable / cultural relations
- AutoEvo / CoEvo
- virtual-session/session-lifecycle ecology
- CoNodeMesh+ / GRAIL+
- identity/fronts/service-principals
- source/provenance/currentness
- research/challenge/prior-art
- education/public explanation
- futures and wake conditions
- accessibility/locality/mobile/XR
- security/privacy/governance rails

The list is open. New domains should first be represented as relations, not automatically minted as repositories.

`DOMAIN_NE_REPOSITORY`

## Session contribution contract

A session may contribute one or more `CoEvoDelta+` objects.

Minimum fields:

`delta_id | session_id | observed_at | domain | subject | relation | epistemic_class | source_refs | target_surfaces | mutation_class | authority_ceiling | confidentiality | confidence | contradictions | tests | expected_benefit | risks | wake_conditions | supersedes | next_receiver`

### Mutation classes

- `OBSERVE` — read-only currentness/evidence.
- `PROPOSE` — durable candidate delta, no target mutation.
- `BRANCH_MUTATE` — create/update reversible candidate branch or PR.
- `REVIEW_CHALLENGE` — critique, contradiction, tests, prior-art.
- `MERGE_LOW_EFFECT` — docs/schema/tests/public-safe metadata where authority and collision checks allow.
- `EFFECT_GATED` — runtime/public outreach/credentials/authority/financial/privacy-sensitive/irreversible effects.

No session receives a broader authority class merely because it can authenticate to GitHub.

`GITHUB_WRITE_ACCESS_NE_GLOBAL_AUTHORITY`

## Domain subscriptions instead of omniscience

Sessions should maintain a bounded subscription set.

Example:

```
CoLanguage session:
  HOT: CoLex, CoGibberTru, CoPriMath projection loss
  WARM: CoTheoryAll, CoIndex
  DIGEST: UX, public highlights
  SLEEP: unrelated runtime lanes
```

GitHub reduces CoCarry+ only when a session reads the relevant current slices rather than re-ingesting the repository universe.

`CURRENTNESS_FOR_ALL_NE_CONTENT_FOR_ALL`

## Cross-repository role map

Current repository names are projections and may evolve.

### Public front doors / exemplars
- `CoCivium/CoCivium` — shared public/currentness/bootstrap/integration surface.
- `EntMent` — AI-first beacon exemplar.
- `CoAura` — interface/projection envelope.
- `CoAcademy` — educational/public learning surface.
- `CoPolitic-site` — separate political/public scaffold; ordinary project evolution does not imply political content authority.

### Index / knowledge / source
- `GIBindex` — term/alias/deprecation/index lineage.
- `CoInsights` — insight/idea atoms and whitepaper fragments.
- `CoSources` — source/provenance projections.
- `MasterPlan` — reviewable strategic/planning artifacts.

### Operations / automation
- `CoSteward` — private operational runbooks/triage.
- `CoToolbelt` — reusable scripts/utilities.
- `CoStacks` — automation/operator experiments.
- `CoRails` — rails/invariants/guardrails.
- `CoShareHub` — navigation/discovery.
- `CoBusMirror` — legacy/public mirror lineage; not sole currentness authority.

### Futures / private stewardship / vaults
- `CoFutures` — futures/probabilistic ledger.
- `.github` — account/repository meta configuration.
- `CoTrustedWindow` — private trusted-human window.
- `CoLegalVault`, `CoCrownJewelVault` — restricted vault roles.
- `TEMP` — public-safe transient inbox only.

`REPOSITORY_NE_ONTOLOGY`

## Domain-to-repo election

A delta chooses its target by semantics, confidentiality, currentness, and receiver need.

Examples:

- new CoTerm relation -> GIBindex candidate + CoCivium public projection if public-safe;
- new research insight -> CoInsights candidate + linked source refs;
- new runtime helper -> CoToolbelt/CoStacks, not README prose;
- new invariant -> CoRails candidate + schema/test if applicable;
- new public UX relation -> CoAura / CoCivium surface docs;
- new strategy -> MasterPlan private candidate, public-safe summary in CoCivium if useful;
- new future hypothesis -> CoFutures private by default;
- new highlight asset -> CoCivium highlight index after public-safe/readiness check.

A cross-cutting relation may legitimately project into several repositories.

`ONE_OBJECT_CAN_HAVE_MANY_REPO_PROJECTIONS`

## README and folder evolution

README files and directory trees are UX/index projections, not semantic truth.

Sessions may propose improvements when they reduce:

- navigation cost;
- ambiguity;
- stale pointers;
- orphan exposure;
- duplicate concepts;
- human attention debt;
- AI bootstrap time.

Do not reorganize directories merely for aesthetic tidiness.

`DIRECTORY_TREE_NE_SEMANTIC_TREE`  
`README_NE_CANON`

## CoTheoryAll / CoPriMath / CoGibberTru

These are especially cross-cutting.

A contribution should preserve:

- source claim;
- epistemic class;
- observer/model/scale/time qualification;
- transformation/projection relation;
- loss/ambiguity;
- contradiction;
- test or discriminating observation where possible;
- public explanation distinct from scientific status.

Mathematical notation or compressed symbolic forms do not gain truth by density.

`FORMALIZATION_NE_VALIDATION`  
`COMPRESSION_NE_TRUTH`

## CoOps+ / CoMythOps+ / humour

Operational, metaphorical, mythical, humorous and memeable projections may all contribute useful structure.

They must remain typed so that a joke, myth, metaphor, operational claim and scientific claim cannot silently collapse into one another.

`METAPHOR_NE_EQUIVALENCE`  
`HUMOUR_NE_EVIDENCE`  
`MYTHIC_PROJECTION_NE_FACTUAL_CLAIM`

## Highlight ecology

A highlight is not “whatever sounds coolest.”

Candidate highlight dimensions:

- public usefulness;
- inspectability;
- evidence/currentness;
- distinctiveness;
- visual/demo value;
- research value;
- remixability/memeability;
- accessibility;
- challengeability;
- readiness.

Highlights may be promoted, demoted, split, or superseded as evidence changes.

`HIGHLIGHT_NE_CANON`

## AutoEvo feedback

Every useful GitHub mutation should be available as training evidence for the mechanisms that selected it:

`delta -> review/test -> merge/reject -> observed outcome -> CoAutoEvo lesson`

Rejected PRs and failed experiments are not waste; they are negative knowledge.

`REJECTED_NE_USELESS`

## Anti-thrash / collision rules

Before writing:

1. refresh target current head;
2. search for existing equivalent relation;
3. bind source refs;
4. classify confidentiality;
5. identify collision domain;
6. prefer branch/PR for cross-cutting or uncertain changes;
7. preserve source history;
8. merge only inside authority/effect ceiling;
9. emit currentness pointer after merge.

When several sessions converge on the same object, prefer fan-in/reconciliation over last-writer-wins.

`NO_NEWEST_WINS`

## What all sessions should know

At minimum:

- GitHub is a shared public/currentness/evolution surface.
- CoStead/private nodes remain private durable fabric.
- account/model memory is not project truth.
- sessions should contribute bounded deltas instead of carrying everything.
- domain subscriptions beat total-context ingestion.
- provider sessions are replaceable embodiments.
- public/main/merged still does not automatically mean canon, CoEx, or scientific validation.

## Implementation currentness

Landed on `main` as bounded candidate surfaces:

1. machine-readable evolution-domain registry -> `ai/evolution-lanes.json`;
2. CoEvoDelta schema -> `schemas/coevo-delta-v0.1.schema.json`;
3. public highlight registry -> `ai/highlight-assets.json`;
4. repo-role/currentness map -> `ai/repo-role-map.json`;
5. R2 logical spawn planner -> `scripts/CoLogicalSpawnPlanner.py` plus `docs/Operations/COLOGICAL_SPAWN_PLANNER_R2.md`;
6. confidentiality-preserving CoSpawn routing -> private/restricted/unknown targets fail closed against public destinations;
7. R3 local-model adapter -> `scripts/CoLocalModelWorkerR3.py` plus `ai/local-model-worker-policy.json`, with loopback-only/public-only/no-effect gates;
8. R4 CoFleetProjection+/RickBar UX contract -> `docs/UX/COFLEET_RICKBAR_R4.md`, schema and policy, with exception-first foregrounding and coverage-qualified counts;
9. R4A deterministic CoFleet projection compiler -> `scripts/CoFleetProjectionCompilerR4A.py` plus `docs/UX/COFLEET_PROJECTION_COMPILER_R4A.md`, compiling bounded lifecycle/spawn inputs without synthesizing missing runtime facts.

CoSteward has also recorded a bounded real R2 execution over four session-authored CoEvo deltas: four logical contracts, zero validation issues, zero live workers, and receiver pickup still unproven. Fresh receiver reconciliation found convergent overlap between the CoOutputContract+ delta and CoFleet R4, but explicitly did not infer exact-plan pickup, integration, or runtime binding. This is empirical planner/reconciliation evidence, not integration.

R2 compiles and deduplicates logical work; it does not create provider sessions or live workers. R3 is adapter-ready, but the X2/Ollama materialization canary remains unrun at this observation boundary. R4 defines the UX/data contract only. R4A can compile a bounded projection, but RickBar receiver/runtime binding and a live global fleet census remain unproven.

`IMPLEMENTED_SURFACE_NE_GLOBAL_ADOPTION`  
`LOGICAL_SPAWN_NE_LIVE_WORKER`  
`PLANNER_EXECUTION_NE_RECEIVER_PICKUP`  
`ADAPTER_READY_NE_RUNTIME_CANARY`  
`FLEET_PROJECTION_NE_FLEET_TOTALITY`  
`RICKBAR_CONTRACT_NE_RUNTIME_BINDING`  
`MODEL_OUTPUT_NE_EFFECT_PERMISSION`  
`MAIN_NE_CANON`

## Next implementation

1. when a direct X2 machine route is available, run one already-installed-model, public-safe, low-effect Ollama canary through R3;
2. exact-hash that candidate output and require receiver readproof plus an independent verifier before any integration;
3. bind R4A output to RickBar through R4B only after a proven receiver/runtime adapter, keeping live counts coverage-qualified and requiring explicit runtime facts for provider/model counts;
4. continue cross-repo bootstrap/domain contracts only where they reduce navigation or routing debt;
5. add fan-in/currentness receipts so concurrent session deltas reconcile instead of becoming last-writer-wins;
6. measure bootstrap-time, duplicate-rate, proof-debt, fan-in debt, receiver backlog, and human-attention reduction as evolution-fabric health signals.

## Rails

`ALL_SESSIONS_CAN_CONTRIBUTE_NE_ALL_SESSIONS_MUTATE_ALL_SURFACES`  
`GITHUB_WRITE_ACCESS_NE_GLOBAL_AUTHORITY`  
`CURRENTNESS_FOR_ALL_NE_CONTENT_FOR_ALL`  
`DOMAIN_NE_REPOSITORY`  
`REPOSITORY_NE_ONTOLOGY`  
`DIRECTORY_TREE_NE_SEMANTIC_TREE`  
`README_NE_CANON`  
`HIGHLIGHT_NE_CANON`  
`FORMALIZATION_NE_VALIDATION`  
`COMPRESSION_NE_TRUTH`  
`METAPHOR_NE_EQUIVALENCE`  
`HUMOUR_NE_EVIDENCE`  
`NO_NEWEST_WINS`
