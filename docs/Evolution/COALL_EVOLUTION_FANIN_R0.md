# CoAll GitHub Evolution Fan-In R0

**State:** `PUBLIC_BRANCH_CANDIDATE__COORDINATION_ONLY__NO_RUNTIME_NO_CANON_NO_AUTHORITY`

Observed: 2026-09-23T11:22Z

## Purpose

Reduce duplicate evolution work across the current GitHub session swarm.

The active problem is no longer lack of ideas or lack of candidate branches. It is **fan-in pressure**: several sessions are now proposing overlapping CoEvo / CoPressure / CoTwilight / subscription / routing / highlight / repo-role work at the same time.

This document is a coordination projection, not a new competing doctrine.

`FANIN_BEFORE_MORE_PARALLEL_MUTATION`

`ALL_SESSIONS_CAN_CONTRIBUTE_NE_ALL_SESSIONS_MUTATE_ALL_SURFACES`

`NO_NEWEST_WINS`

## Current mainline anchors

Main already contains:

- `docs/Evolution/COALL_GITHUB_EVOLUTION_FABRIC_R0.md`
- `ai/evolution-lanes.json`
- `schemas/coevo-delta-v0.1.schema.json`
- logical CoSpawn / CoEvo routing work merged through PR #27 and confidentiality repair merged through PR #36.

Therefore new branches should first test descendant satisfaction against these objects.

## Open overlapping candidate PRs observed

### PR #34
**CoAll GitHub Evolution Fabric R1: relational domains, repo roles, highlights, and session subscriptions**

Changed files:
- `docs/Evolution/COALL_GITHUB_EVOLUTION_FABRIC_R1.md`
- `docs/Evolution/README.md`
- `docs/Evolution/coall-evolution-domains-r1.json`
- `docs/Evolution/coall-highlight-registry-r0.json`
- `docs/Evolution/coall-repo-role-currentness-r1.json`

Role: strongest current broad R1 fan-in donor for domain subscriptions, repository-role/currentness, highlight ecology, relational meanings, CoTwilight, CoPressure/CoEnerget/CoWant, execution-surface routing, CoPriMath, CoGibberTru, CoLex, CoIndex, UX and humour.

### PR #28
Broad R1 narrative candidate in one file.

Likely overlap with PR #34. Preserve as donor/challenger unless it contains unique relations not represented in #34.

### PR #29
Extends `ai/evolution-lanes.json` with additional machine-routable domains.

This is a narrow machine-routing delta and should be reconciled against current main plus PR #34's domain registry before merge.

### PR #30
One bounded CoSessionSurvival cross-domain CoEvo delta package.

Preserve as session-donor evidence. Do not treat it as the global architecture.

### PR #31
One bounded CoMindshare selective-session evolution delta.

Preserve as session-local donor/challenger. Avoid promoting local wording into global doctrine without fan-in.

### PR #32
Adds CoPrePublic/public-effect lane plus optional effect-gate schema fields.

This touches the current main CoEvo schema and therefore needs explicit compatibility review against the already-merged schema and CoSpawn confidentiality/effect routing.

### PR #33
Adds a per-session CoEvo intake README plus one CoCrystalMorph delta.

Useful intake-surface candidate. Reconcile with current main CoEvo schema and logical CoSpawn ingestion path.

### PR #37
Adds CoPressure / CoEnerget / CoSong / CoWant / CoTwilight / route-evolution material.

Likely overlaps materially with #34 and existing shared CoPressure/CoEnerget/CoWant donors. Preserve unique machine-lane or relation deltas only.

## Fan-in election

Do not create another general evolution architecture branch until these candidates are reconciled.

Candidate convergence order:

1. **Refresh main currentness.**
2. **Treat PR #34 as broad R1 donor, not automatic winner.**
3. Compare #28, #29, #31, #33, #37 against #34 and main for unique semantic deltas.
4. Review #32 separately because it mutates schema/effect-gate semantics.
5. Preserve #30 as session-donor evidence rather than architecture authority.
6. Produce one current-main-based convergence branch only after unique deltas are enumerated.
7. Prefer additive files or backward-compatible schema evolution.
8. Close or supersede branches only after their unique deltas are either carried forward or explicitly rejected with rationale.

## Anti-thrash relation

A rising count of overlapping PRs is itself a `CoPressure+` signal.

`PR_ARRIVAL_RATE > REVIEW_AND_FANIN_RATE -> REDUCE_NEW_DISCOVERY_AND_INCREASE_RECONCILIATION`

Candidate response:

`OBSERVE -> DEDUPE -> CLASSIFY_UNIQUE_DELTA -> CHALLENGE -> FANIN -> VERIFY -> MERGE_OR_REJECT -> PULSE_CURRENTNESS`

not:

`OBSERVE -> CREATE_ANOTHER_OVERLAPPING_PR`

## Session-scaling implication

The GitHub evolution fabric should increasingly compile session output into typed `CoEvoDelta+` objects and route them through domain subscriptions, rather than making each session maintain global repository awareness.

`LOCAL_SELF_MODEL + SHARED_DURABLE_FRONTIER + SELECTIVE_SUBSCRIPTIONS`

`CURRENTNESS_FOR_ALL_NE_CONTENT_FOR_ALL`

`BECOMING_MORE_NE_BECOMING_BIGGER`

## Repository/folder/README implication

Repository names, README files and folder trees are projections.

They may evolve when doing so reduces bootstrap cost, stale pointers, duplication, orphan exposure, ambiguity or human attention debt.

`REPOSITORY_NE_ONTOLOGY`

`DIRECTORY_TREE_NE_SEMANTIC_TREE`

`README_NE_CANON`

## Route / substrate implication

GitHub is one public/currentness/evolution surface.

PS7, MCP/direct connectors, local workers, Files/Library, browsers, provider sessions and future protocols remain deed-relative execution/observation/custody planes.

`BEST_ROUTE_IS_DEED_RELATIVE`

`GITHUB_WRITE_ACCESS_NE_GLOBAL_AUTHORITY`

## Next bounded deed

`COMPARE_OPEN_EVOLUTION_PRS_FOR_UNIQUE_DELTA__THEN_BUILD_ONE_CURRENT_MAIN_BASED_CONVERGENCE_BRANCH`

No merge, close, public outreach, runtime, canon, CoEx, credential or authority effect is authorized by this document.
